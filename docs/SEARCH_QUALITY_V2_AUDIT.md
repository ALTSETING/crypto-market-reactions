# Search Quality V2 audit

Queries: **50**. Metadata-only candidate index fields: title, display title, source, category, record type, and related assets. Article bodies were not indexed.

Queries with no metadata match: **0** ([]). Ranked top-10 candidates and manual-review placeholders are in `reports/SEARCH_QUALITY_V2_AUDIT.csv`.
